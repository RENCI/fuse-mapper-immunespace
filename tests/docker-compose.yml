version: '3.8'
services:
  
  fuse-mapper-immunespace-test:
    build:
      context: .
      dockerfile: tests/Dockerfile
    command: ["-s", "-vv", "-x"] # , "-k", "test_api_spec5"]
    environment:
      INPUT_DIR: /shared
      OUTPUT_DIR: /shared
    volumes:
      - $SHARED_DIR:/shared
    depends_on:
      - fuse-mapper-immunespace



    
    
